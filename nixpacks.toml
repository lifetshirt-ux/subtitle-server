[phases.setup]
nixPkgs = [
  "ffmpeg",
  "pkg-config",
  "cairo",
  "pango",
  "libpng",
  "libjpeg",
  "giflib",
  "librsvg",
  "pixman"
]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"
